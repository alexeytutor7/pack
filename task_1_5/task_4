loguru==0.7.2 — нужна для красивого и удобного логирования. Помогает записывать ошибки и события в программе.
openpyxl==3.1.5 — работает с Excel-файлами. Можно читать, создавать и изменять таблицы.
playwright==1.48.0 — используется для автоматизации браузера.
python-dotenv==1.0.1 — помогает загружать переменные из файла .env, например, пароли или ключи.
mm-okx==0.1.0 — библиотека для работы с биржей OKX.
requests==2.32.3 — позволяет отправлять запросы к сайтам или API, чтобы получать данные.
web3==7.5.0 — используется для взаимодействия с блокчейном Ethereum.

pip install -r requirements.txt

pip install loguru==0.7.2
pip install openpyxl==3.1.5
pip install playwright==1.48.0
pip install python-dotenv==1.0.1
pip install mm-okx==0.1.0
pip install requests==2.32.3
pip install web3==7.5.0

mm-okx: Это неизвестная библиотека, поэтому с ней может быть опасно работать.
Есть риск столкнуться с дрейном или раскрытием чувствительной информации, так как у нее нет описания проекта,
низкий уровень проверки безопасности и нет отзывов от сообщества.
